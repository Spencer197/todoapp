<%= render 'shared/page_title', title: "#{ @user.name }'s Profile Page" %>
<div class="row">
  <div class="col-md-4 col-md-offset-5 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4 align="center"><%= @user.name %></h4><%#Shows user's name under Gravatar image.%>
<%= render 'todos/todo', obj: @user.todos %><%# Renders _todos.html.erb partial using its path, then defines the object to be used to render a user's todos. %>